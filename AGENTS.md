## Objective
- Mantener sismo.lat (prod) estable; login Google end-to-end. Fase 1 (COMPLETADA): corregir flujo "ceder cupo", visibilidad de inscritos, tour inicial, bugs "dos páginas"/tour aleatorio, y unificar /mis-actividades. Fase 2 (COMPLETADA): limpiar actividades, seeds demo del tour, guías de onboarding por subpágina (primer acceso), zonas fijas a 7 municipios, y paginar el feed. Fase 3 (EN CURSO): corregir render de vistas semana/día para actividades largas o que cruzan la medianoche.

## Important Details
- App: FastAPI (`apps/api`) + Next.js 15 (`apps/web`), Postgres + Redis.
- Auth cookie `sismo_session` = `base64url(json).hmac_sha256(secret, encoded)`, payload `{user_id,role,status,iat,exp,jti}`; secret = `SISMO_SESSION_SECRET` (Docker secret `infra/secrets/sismo_session_secret`, 64 chars). CSRF: cookie `XSRF-TOKEN` + header `X-CSRF-Token`.
- **Contraseña de BD (CRÍTICO — ver `docs/DB_PASSWORD_GOTCHA.md`):**
  - `SISMO_DB_PASSWORD=njiB0vws*9DCIUnCVCpCbKDHj40#yqxn` (en `infra/.env`; el `#` se conserva bien).
  - La API NO recibe la pw como env var: viene de Docker secret `infra/secrets/sismo_db_password` (montado en `/run/secrets/...`, exportado a env por `docker-entrypoint.sh`). El fallback `config.py` (`db_password="sismo"`) solo aplica si el secret no está montado.
  - Postgres rol `sismo` password DEBE coincidir exactamente con el valor del secret (también = `POSTGRES_PASSWORD`).
  - **TRAMPAS (no repetir):** (1) NUNCA `ALTER ROLE sismo WITH PASSWORD 'sismo'`. (2) NUNCA recrear postgres con `docker-compose.dev.yml` (usa `sismo_pgdata_dev` + red `sismo-dev`). Usar override estándar (`sismo_pgdata` + red `sismo`).
  - Diagnóstico sobre la red `sismo` (NO 127.0.0.1, que usa trust): `PW=$(cat infra/secrets/sismo_db_password); docker run --rm --network sismo postgres:16-alpine psql "postgresql://sismo:$PW@postgres:5432/sismo" -tAc "SELECT 1"`.
- Dev admin fijo: `DEV_ADMIN_ID = 11111111-1111-1111-1111-111111111111`.
- Red `sismo`: `infra-api-1`, `infra-web-1` (alias `web` — ÚNICO), `infra-postgres-1` (alias `postgres`), `infra-redis-1` (alias `redis`), `infra-cloudflared-1`. **CRÍTICO: el alias `web` debe pertenecer a UN SOLO container** (`infra-web-1`). Nunca levantar un segundo web con el mismo alias (causaba "dos páginas" / tour aleatorio).
- Web routes: `/voluntarios`, `/voluntarios/crear`, `/mis-actividades`, `/admin`, `/login`, `/auth/finish`, `/voluntarios/[id]`, `/voluntarios/[id]/admin`, `/voluntarios/[id]/editar`, `/perfil`. Login button = `<a href="{NEXT_PUBLIC_API_URL}/api/v1/auth/login">` → 302 a `accounts.google.com`.
- **Volúmenes que SE CONSERVAN (decisión usuario):** `infra_proxy_cache_data` (`/app/proxy_cache`, fotos blob, evita 429 de Google) y `infra_media_data` (`/data/media`). NO borrar. `docker builder prune -f` SÍ es seguro (liberó 20.57GB 2026-07-18).
- **Hosts:** app `sismo.lat` (web), API `api.sismo.lat` (alias `api`). `sismo.lat/api/*` → 404 esperado (web no proxy en prod). Login real: `https://api.sismo.lat/api/v1/auth/login` → 302 Google.
- **Rebuild prod:** `cd infra && docker compose -f docker-compose.yml build --no-cache web` (o `api web`) + `docker compose -f docker-compose.yml up -d --force-recreate --no-deps web`. Web NO monta volúmenes de `node_modules`/`.next` → rebuild limpio. NO hay node/npm en el host (solo docker); typecheck/build van por docker.
- **Gotchas de build:** `useSession` se importa de `@/components/session-provider` (NO `@/lib/auth/session`, server-only con `next/headers` → rompe build). `ZoneFilter` prop `zones: readonly string[]` (pasar `ZONES` const readonly).
- **Seed demo:** el container `infra-api-1` `/app` es copia de imagen (NO montado). Para re-seed: `docker cp apps/api/scripts/seed_demo_activities.py infra-api-1:/app/seed_demo_activities.py` y luego `docker exec -e SISMO_DB_PASSWORD="$PW" infra-api-1 python /app/seed_demo_activities.py --force`.
- **`ZONES` const** en `apps/web/lib/zones.ts`: `["Caracas","Guatire","Guarenas","La Guaira","Caucagua","Los Teques","El Junquito"]`. Usado en `zone-filter.tsx`, `crear-activity-client.tsx`, `editar-activity-client.tsx`, `voluntarios/page.tsx`.
- **`PageGuide`** en `apps/web/components/page-guide.tsx` (flag localStorage `sismo_guide_<id>`), inyectado en `crear`, `mis-actividades`, `perfil`, `voluntarios/[id]` (detalle), `admin` (gated `role==="admin"`). El feed `/voluntarios` conserva su propio tour.
- **Paginación feed:** API `activities.py:list_activities` acepta `limit` (default 20, clamp 1–50) y `offset` (default 0). Web `voluntarios/page.tsx` usa `FEED_LIMIT=20`, `offsetRef`, `hasMore`, botón "Cargar más".
- **Demo seed (4 actividades, `demo_until` 2026-07-21, TTL 3d):** "Ir a centro de acopio a ordenar insumos" (Caracas), "Jornada recreativa para niños en refugios" (Guatire), "Entrega de insumos en el centro comunitario" (Guarenas), "Apoyo a proyecto de reciclaje de ropa en centro de acopio" (La Guaira). Feed pide `include_demo=true` SIEMPRE (visibles para todos).
- **Vistas de calendario:** `activity-week-view.tsx` (DEFAULT_START_HOUR=6, DEFAULT_END_HOUR=22, MAX_AXIS_END=30, HOUR_WIDTH=100, LANE_HEIGHT=44, BLOCK_HEIGHT=36, packLanes) y `activity-gantt-view.tsx` (día; `getActivityBlock` null si no sameDay; startHour/duration desde `estimated_duration_min` o `end_time`). Ambas expanden el eje temporal hasta `MAX_AXIS_END=30` para actividades que terminan después de medianoche (antes se clavaba en 24 y el bloque se recortaba "plano" por `overflow-hidden`). `formatHour(h)` maneja horas >24 ("1 AM", etc.).
- **GitHub:** alertas dependabot (1 high, 1 moderate) en default branch — NO de esta sesión; revisar antes de next deploy.
- Test runner aislado: `docker compose -f infra/docker-compose.dev.yml run --rm test-api` (BD `sismo_test`). Caveat: fixture `truncate` no limpia `users`; hay que dropear `sismo_test` entre archivos. `openpyxl` requerido.

## Work State
- Completed:
  - **Fase 1:** Fix login Google (rol `sismo` password = secret real). Restore BD live. Fixes OAuth 500→error, autocompletado IA (ce28476). Bug A `_enrolled_activity_ids_subquery` excluye active/ceded. Bug B2 `list_attendees` sin 403 (email solo creador/SEP/admin). Flujo ceder cupo (transfer_membership/pending_transfer/accept/reject, `GET /activities/ceded`, `check_membership`, join bloquea rejoin si ceded). Tests PASS (ceder 9/9, visibilidad 15/15, qa 30/30). Fix "dos páginas"/tour aleatorio (eliminado `sismo-public-web-1`, `infra-web-1` único alias `web`). Bug 1 cedido+"Unirme" (feed trae /activities/ceded → cededIds; card/modal/join-button muestran "Cupo cedido"). Bug 2 tour gated a `/voluntarios`. Bug 3 unificar /mis-actividades (3 secciones, eliminada ruta huérfana `mis-actividades/todas`). Deploy `--no-cache` verificado.
  - **Fase 2:** DELETE de todas las actividades (users intactos). Seed reescrito a 4 actividades demo + re-seed `--force`. `PageGuide` reutilizable + inyección en 5 subpáginas; build fix import useSession (`@/components/session-provider`); texto guía `crear` corregido; feed siempre `include_demo=true`; guía `perfil` aclara foto opcional. Commits `d1022f2`, `e4f9b2e`. Zonas: 7 municipios fijos vía `lib/zones.ts`; `zone-filter.tsx` toma lista fija + counts; formularios usan `ZONES`; build fix readonly tuple. Cache: `docker builder prune -f` liberó 20.57GB; volúmenes proxy_cache/media conservados. Perf: serialización sin N+1; paginación feed (API limit/offset + web "Cargar más"). Commit `f306e09` + push `origin/main`.
  - **Fase 3 (bug render):** `activity-week-view.tsx` y `activity-gantt-view.tsx` extendían el eje solo hasta `Math.min(24, ...)` → actividad larga/overnight se recortaba "plana" por `overflow-hidden`. Cambiado a `Math.min(MAX_AXIS_END=30, ...)` para que el eje se expanda y el bloque se vea completo con bordes redondeados (sin corte recto). Agregada `formatHour(h)` para etiquetas >24 ("1 AM" etc.). Rebuild `infra-web` `--no-cache` + recreate `infra-web-1` (2026-07-18, Up). Verificado: container Ready.
- Active:
  - Residual JoinButton: `voluntarios/[id]/page.tsx` `<JoinButton>` hace la membership fetch y `setStatus("left")` para cedido (debería `"ceded"`); podría mostrar "Unirme" en actividad cedida. El feed/modal ya usan `isCeded` y están bien.
- Blocked: (none)

## Next Move
1. Verificar en vivo (sismo.lat) que una actividad larga y un caso overnight (si existe) se rendericen expandidos y sin corte recto en vistas semana y día.
2. (Opcional) JoinButton `setStatus("ceded")` en vez de `"left"` para cedidos.
3. Antes del próximo deploy: revisar las 2 alertas dependabot (1 high, 1 moderate) en GitHub.
4. (none más) — flujo ceder/tour/paginación/zonas/guías entregados; verificación en vivo del usuario.
