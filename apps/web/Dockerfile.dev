FROM node:22-alpine
WORKDIR /app
COPY package.json ./
RUN --mount=type=cache,target=/root/.npm \
    npm install --legacy-peer-deps
COPY . .
# Puerto abstracto: el puerto real lo inyecta el compose con --port
EXPOSE 3001
CMD ["npm", "run", "dev"]
